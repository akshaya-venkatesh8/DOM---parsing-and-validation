# SAX---parsing-and-validation
This repository will parse and validate an address.xml file against a schema "address.xsd". 
If the document is valid an address object is returned else error is displayed.
